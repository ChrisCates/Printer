import { connect } from 'react-redux';
import { WorldComponent } from './world.component';

export const WorldState = state => ({

});

export const WorldActions = ({

});

export const World = connect(WorldState, WorldActions)(WorldComponent);