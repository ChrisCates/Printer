import { connect } from 'react-redux';
import { IndexOverviewComponent } from './index.overview.component';

export const IndexOverviewState = state => ({

});

export const IndexOverviewActions = ({

});

export const IndexOverview = connect(IndexOverviewState, IndexOverviewActions)(IndexOverviewComponent);