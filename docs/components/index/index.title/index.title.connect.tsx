import { connect } from 'react-redux';
import { IndexTitleComponent } from './index.title.component';

export const IndexTitleState = state => ({

});

export const IndexTitleActions = ({

});

export const IndexTitle = connect(IndexTitleState, IndexTitleActions)(IndexTitleComponent);