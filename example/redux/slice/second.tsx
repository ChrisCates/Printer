import { createSlice } from '@reduxjs/toolkit'

export const secondInitialState = {}

export const secondSlice = createSlice({
  name: 'second',
  initialState: secondInitialState,
  reducers: {}
})

export const {} = secondSlice.actions
